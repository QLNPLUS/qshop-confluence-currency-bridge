# 更新日志

## 1.1.0

### Forge 1.20.1

- NPC 交易界面的“卖出”提示显示当前 NPC 心情系数调整后的收购金额，与实际结算金额一致。
- 修复 Forge 1.20.1 下加载 Confluence NPC 交易界面时的启动错误。

## 1.0.0

首个版本，支持 Forge 1.20.1 与 NeoForge 1.21.1。

- 新增：把一个 QShop 货币 id 绑定到 Confluence 的钱币上。绑定货币的余额实时读取玩家
  身上的钱币（背包 + 钱币栏，可选包含存钱罐），QShop 的购买/出售/指令/任务奖励
  直接增删钱币，不再维护一份独立余额。
- 新增：出售箱显示绑定货币的价格时改用 Confluence 面额格式
  （例如 `1234567` 显示为 `1 铂金币 23 金币 45 银币 67 铜币`），
  自动出售与离线补发的提示消息同样处理。
- 新增：玩家离线时的绑定货币入账先记在服务器账面，登录时以钱币补发。
- 新增：`/qshopconfluence status` 自检指令，同时显示 Confluence 钱币与 QShop 读到的余额。
- 兼容：未安装 Confluence 或出售箱时模组仍可正常加载，对应功能自动关闭。
- 兼容：绑定货币默认不受 QShop 死亡货币留存规则影响，避免与 Confluence 自身的
  死亡掉落重复扣除。
